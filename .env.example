TG_BOT_TOKEN=
OR_API_KEY=
DEEPINFRA_KEY=
RUST_LOG=info,sqlx=warn,teloxide=info

# Только для локальной разработки с sqlx-cli (миграции, prepare).
# В рантайме бот использует sqlite.path из config.toml.
DATABASE_URL=sqlite:./data/bot.db
